spring:
  application:
    name: suda-eda
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/studs
    username: postgres
    password: postgres
springdoc:
  swagger-ui:
    path: '/swagger-ui'